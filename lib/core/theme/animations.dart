import 'package:flutter/material.dart';

const Duration microDuration = Duration(milliseconds: 150);
const Duration transitionDuration = Duration(milliseconds: 250);

const Curve animationCurve = Curves.easeInOut;

const double fadeBegin = 0.0;
const double fadeEnd = 1.0;

const Offset slideOffset = Offset(0, 0.05);

const double scaleBegin = 0.97;
const double scaleEnd = 1.0;